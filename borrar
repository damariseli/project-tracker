<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tablero de proyectos</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #F7F6F3;
      --surface: #FFFFFF;
      --surface-2: #F0EEE9;
      --border: rgba(0,0,0,0.08);
      --border-md: rgba(0,0,0,0.14);
      --text-primary: #1A1917;
      --text-secondary: #6B6860;
      --text-tertiary: #A09D97;
      --accent: #2563EB;
      --accent-bg: #EFF4FF;
      --green: #16A34A;
      --green-bg: #F0FDF4;
      --amber: #D97706;
      --amber-bg: #FFFBEB;
      --purple: #7C3AED;
      --purple-bg: #F5F3FF;
      --red: #DC2626;
      --radius-sm: 6px;
      --radius-md: 10px;
      --radius-lg: 14px;
      --shadow-sm: 0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04);
      --shadow-md: 0 4px 12px rgba(0,0,0,0.08);
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --bg: #111110;
        --surface: #1C1C1A;
        --surface-2: #252522;
        --border: rgba(255,255,255,0.08);
        --border-md: rgba(255,255,255,0.14);
        --text-primary: #F5F4F0;
        --text-secondary: #9B9890;
        --text-tertiary: #5C5A56;
        --accent: #60A5FA;
        --accent-bg: #1E3A5F;
        --green: #4ADE80;
        --green-bg: #052E16;
        --amber: #FCD34D;
        --amber-bg: #2D1A00;
        --purple: #A78BFA;
        --purple-bg: #2D1A5E;
      }
    }

    body {
      font-family: 'Inter', system-ui, sans-serif;
      background: var(--bg);
      color: var(--text-primary);
      min-height: 100vh;
      font-size: 14px;
      line-height: 1.5;
    }

    /* Layout */
    .app { display: grid; grid-template-columns: 220px 1fr; min-height: 100vh; }
    @media (max-width: 720px) { .app { grid-template-columns: 1fr; } }

    /* Sidebar */
    .sidebar {
      background: var(--surface);
      border-right: 1px solid var(--border);
      padding: 1.5rem 1rem;
      position: sticky;
      top: 0;
      height: 100vh;
      overflow-y: auto;
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }
    @media (max-width: 720px) { .sidebar { display: none; } }

    .sidebar-logo {
      font-size: 13px;
      font-weight: 600;
      color: var(--text-primary);
      padding: 0.25rem 0.75rem 1.25rem;
      letter-spacing: -0.01em;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .logo-dot {
      width: 8px; height: 8px; border-radius: 50%;
      background: var(--accent);
    }

    .nav-item {
      display: flex; align-items: center; gap: 10px;
      padding: 0.5rem 0.75rem;
      border-radius: var(--radius-sm);
      font-size: 13px;
      color: var(--text-secondary);
      cursor: pointer;
      border: none; background: none; width: 100%; text-align: left;
      transition: background 0.15s, color 0.15s;
    }
    .nav-item:hover { background: var(--surface-2); color: var(--text-primary); }
    .nav-item.active { background: var(--accent-bg); color: var(--accent); font-weight: 500; }
    .nav-icon { font-size: 16px; opacity: 0.8; }

    .nav-section-label {
      font-size: 11px; font-weight: 600; letter-spacing: 0.06em;
      color: var(--text-tertiary); padding: 1rem 0.75rem 0.4rem;
      text-transform: uppercase;
    }

    /* Main */
    .main { padding: 2rem; overflow-x: hidden; }
    @media (max-width: 720px) { .main { padding: 1.25rem; } }

    .page { display: none; }
    .page.active { display: block; }

    /* Page header */
    .page-header {
      display: flex; align-items: center; justify-content: space-between;
      margin-bottom: 1.75rem; gap: 1rem; flex-wrap: wrap;
    }
    .page-title { font-size: 20px; font-weight: 600; letter-spacing: -0.02em; }
    .page-subtitle { font-size: 13px; color: var(--text-secondary); margin-top: 2px; }

    /* Metrics */
    .metrics { display: grid; grid-template-columns: repeat(auto-fit, minmax(130px, 1fr)); gap: 10px; margin-bottom: 1.75rem; }
    .metric-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius-md);
      padding: 1rem;
      box-shadow: var(--shadow-sm);
    }
    .metric-label { font-size: 12px; color: var(--text-secondary); margin-bottom: 4px; }
    .metric-val { font-size: 24px; font-weight: 600; color: var(--text-primary); line-height: 1; }

    /* Project cards */
    .projects-grid { display: flex; flex-direction: column; gap: 10px; }

    .project-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius-lg);
      padding: 1.125rem 1.25rem;
      box-shadow: var(--shadow-sm);
      transition: box-shadow 0.15s;
    }
    .project-card:hover { box-shadow: var(--shadow-md); }

    .card-top { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; margin-bottom: 10px; }
    .card-title-row { display: flex; align-items: center; gap: 8px; }
    .status-dot { width: 7px; height: 7px; border-radius: 50%; flex-shrink: 0; margin-top: 2px; }
    .dot-active { background: var(--green); }
    .dot-review { background: var(--purple); }
    .dot-paused { background: var(--amber); }
    .card-title { font-size: 14px; font-weight: 500; }

    .badge {
      font-size: 11px; font-weight: 500;
      padding: 2px 8px; border-radius: 20px;
      white-space: nowrap;
    }
    .badge-leader { background: var(--accent-bg); color: var(--accent); }
    .badge-collab { background: var(--green-bg); color: var(--green); }

    .card-meta { display: flex; gap: 14px; font-size: 12px; color: var(--text-secondary); margin-bottom: 10px; flex-wrap: wrap; }
    .card-meta-item { display: flex; align-items: center; gap: 4px; }

    .progress-track { height: 3px; background: var(--surface-2); border-radius: 4px; overflow: hidden; margin-bottom: 12px; }
    .progress-fill { height: 100%; border-radius: 4px; transition: width 0.3s; }

    .tasks-list { display: flex; flex-direction: column; gap: 2px; }
    .task-row {
      display: flex; align-items: center; gap: 8px;
      padding: 5px 6px; border-radius: var(--radius-sm);
      transition: background 0.1s;
    }
    .task-row:hover { background: var(--surface-2); }
    .task-row input[type=checkbox] {
      width: 14px; height: 14px;
      accent-color: var(--accent);
      cursor: pointer; flex-shrink: 0;
    }
    .task-label {
      font-size: 13px; color: var(--text-secondary);
      cursor: pointer; flex: 1;
    }
    .task-row.done .task-label { text-decoration: line-through; color: var(--text-tertiary); }

    .card-actions { display: flex; gap: 6px; margin-top: 10px; padding-top: 10px; border-top: 1px solid var(--border); }
    .btn-ghost-sm {
      display: flex; align-items: center; gap: 5px;
      font-size: 12px; color: var(--text-secondary);
      background: none; border: 1px solid var(--border);
      border-radius: var(--radius-sm); padding: 4px 10px;
      cursor: pointer; transition: all 0.12s;
    }
    .btn-ghost-sm:hover { background: var(--surface-2); color: var(--text-primary); border-color: var(--border-md); }

    /* Buttons */
    .btn-primary {
      display: inline-flex; align-items: center; gap: 6px;
      background: var(--text-primary); color: var(--bg);
      border: none; border-radius: var(--radius-sm);
      padding: 7px 14px; font-size: 13px; font-weight: 500;
      cursor: pointer; transition: opacity 0.15s;
    }
    .btn-primary:hover { opacity: 0.85; }
    .btn-secondary {
      display: inline-flex; align-items: center; gap: 6px;
      background: none; color: var(--text-primary);
      border: 1px solid var(--border-md); border-radius: var(--radius-sm);
      padding: 7px 14px; font-size: 13px; font-weight: 500;
      cursor: pointer; transition: background 0.12s;
    }
    .btn-secondary:hover { background: var(--surface-2); }

    /* Modal */
    .modal-backdrop {
      display: none; position: fixed; inset: 0;
      background: rgba(0,0,0,0.4); z-index: 200;
      align-items: center; justify-content: center; padding: 1rem;
    }
    .modal-backdrop.open { display: flex; }
    .modal {
      background: var(--surface); border: 1px solid var(--border);
      border-radius: var(--radius-lg); padding: 1.5rem;
      width: 100%; max-width: 460px;
      box-shadow: var(--shadow-md);
    }
    .modal-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1.25rem; }
    .modal-title { font-size: 15px; font-weight: 600; }
    .modal-close {
      background: none; border: none; cursor: pointer;
      color: var(--text-secondary); font-size: 18px; line-height: 1;
      padding: 2px; border-radius: var(--radius-sm);
    }
    .modal-close:hover { color: var(--text-primary); }

    .form-group { margin-bottom: 14px; }
    .form-label { font-size: 12px; font-weight: 500; color: var(--text-secondary); display: block; margin-bottom: 5px; }
    .form-input, .form-select, .form-textarea {
      width: 100%; padding: 8px 10px;
      border: 1px solid var(--border-md); border-radius: var(--radius-sm);
      background: var(--surface); color: var(--text-primary);
      font-family: inherit; font-size: 13px;
      outline: none; transition: border-color 0.15s;
    }
    .form-input:focus, .form-select:focus, .form-textarea:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(37,99,235,0.1);
    }
    .form-textarea { min-height: 72px; resize: vertical; }
    .progress-row { display: flex; align-items: center; gap: 10px; }
    .progress-row input[type=range] { flex: 1; accent-color: var(--accent); }
    .progress-row span { font-size: 13px; font-weight: 500; min-width: 36px; }

    .modal-footer { display: flex; gap: 8px; justify-content: flex-end; margin-top: 1.25rem; }

    /* Agenda */
    .agenda-section-title { font-size: 12px; font-weight: 600; color: var(--text-tertiary); letter-spacing: 0.05em; text-transform: uppercase; margin-bottom: 10px; margin-top: 1.5rem; }
    .agenda-section-title:first-child { margin-top: 0; }

    .event-row {
      display: flex; gap: 14px; align-items: flex-start;
      padding: 10px 0; border-bottom: 1px solid var(--border);
    }
    .event-row:last-child { border-bottom: none; }
    .event-time { font-size: 12px; color: var(--text-secondary); min-width: 72px; padding-top: 1px; }
    .event-body { flex: 1; }
    .event-title { font-size: 13px; font-weight: 500; }
    .event-sub { font-size: 12px; color: var(--text-secondary); margin-top: 1px; }
    .event-tag {
      font-size: 11px; padding: 2px 7px; border-radius: 20px;
      margin-top: 4px; display: inline-block;
    }

    .add-event-form {
      background: var(--surface); border: 1px solid var(--border);
      border-radius: var(--radius-md); padding: 1rem; margin-top: 1.25rem;
    }
    .add-event-title { font-size: 13px; font-weight: 500; margin-bottom: 10px; }
    .event-input-row { display: flex; gap: 8px; flex-wrap: wrap; }
    .event-input-row .form-input { flex: 2; min-width: 140px; }

    /* Chat */
    .chat-container {
      background: var(--surface); border: 1px solid var(--border);
      border-radius: var(--radius-lg); overflow: hidden;
      display: flex; flex-direction: column;
      height: calc(100vh - 180px); min-height: 400px;
    }
    .chat-messages { flex: 1; overflow-y: auto; padding: 1.25rem; display: flex; flex-direction: column; gap: 12px; }

    .msg { max-width: 82%; }
    .msg-assistant { align-self: flex-start; }
    .msg-user { align-self: flex-end; }
    .msg-bubble {
      padding: 10px 14px; border-radius: var(--radius-md);
      font-size: 13px; line-height: 1.6;
    }
    .msg-assistant .msg-bubble { background: var(--surface-2); color: var(--text-primary); border-radius: var(--radius-md) var(--radius-md) var(--radius-md) 4px; }
    .msg-user .msg-bubble { background: var(--text-primary); color: var(--bg); border-radius: var(--radius-md) var(--radius-md) 4px var(--radius-md); }
    .msg-time { font-size: 11px; color: var(--text-tertiary); margin-top: 3px; }
    .msg-user .msg-time { text-align: right; }

    .chat-quick { padding: 0.75rem 1.25rem; border-top: 1px solid var(--border); display: flex; gap: 6px; flex-wrap: wrap; }
    .quick-chip {
      font-size: 12px; padding: 4px 10px;
      border: 1px solid var(--border-md); border-radius: 20px;
      background: none; color: var(--text-secondary);
      cursor: pointer; transition: all 0.12s;
    }
    .quick-chip:hover { background: var(--surface-2); color: var(--text-primary); }

    .chat-input-area { padding: 1rem 1.25rem; border-top: 1px solid var(--border); display: flex; gap: 8px; }
    .chat-input-area input { flex: 1; }
    .chat-send {
      padding: 8px 14px; background: var(--text-primary); color: var(--bg);
      border: none; border-radius: var(--radius-sm); font-size: 13px;
      cursor: pointer; transition: opacity 0.15s; display: flex; align-items: center; gap: 5px;
    }
    .chat-send:hover { opacity: 0.85; }
    .chat-thinking { font-style: italic; color: var(--text-tertiary); font-size: 12px; padding: 8px 14px; }

    /* API key notice */
    .api-notice {
      background: var(--amber-bg); border: 1px solid var(--amber);
      border-radius: var(--radius-md); padding: 0.875rem 1rem;
      font-size: 13px; color: var(--amber); margin-bottom: 1.25rem;
      display: flex; gap: 8px; align-items: flex-start;
    }
    .api-notice a { color: var(--amber); font-weight: 500; }

    /* Empty */
    .empty-state { text-align: center; padding: 3rem 1rem; color: var(--text-tertiary); }
    .empty-icon { font-size: 32px; margin-bottom: 0.5rem; opacity: 0.5; }
    .empty-text { font-size: 14px; }

    /* Mobile nav */
    .mobile-nav {
      display: none; position: fixed; bottom: 0; left: 0; right: 0;
      background: var(--surface); border-top: 1px solid var(--border);
      padding: 0.5rem; z-index: 100;
      justify-content: space-around;
    }
    @media (max-width: 720px) { .mobile-nav { display: flex; } .main { padding-bottom: 80px; } }
    .mobile-nav-item {
      display: flex; flex-direction: column; align-items: center; gap: 3px;
      padding: 6px 12px; border-radius: var(--radius-sm);
      border: none; background: none; cursor: pointer; color: var(--text-secondary);
      font-size: 10px;
    }
    .mobile-nav-item.active { color: var(--accent); }
    .mobile-nav-item svg { width: 20px; height: 20px; }

    /* Scrollbar */
    ::-webkit-scrollbar { width: 6px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb { background: var(--border-md); border-radius: 3px; }
  </style>
</head>
<body>

<div class="app">

  <!-- Sidebar -->
  <aside class="sidebar">
    <div class="sidebar-logo">
      <span class="logo-dot"></span>
      Mis proyectos
    </div>

    <div class="nav-section-label">Vistas</div>
    <button class="nav-item active" onclick="navigate('tablero', this)">
      <span class="nav-icon">⊞</span> Tablero
    </button>
    <button class="nav-item" onclick="navigate('agenda', this)">
      <span class="nav-icon">◷</span> Agenda
    </button>
    <button class="nav-item" onclick="navigate('asistente', this)">
      <span class="nav-icon">✦</span> Asistente
    </button>

    <div class="nav-section-label" style="margin-top:auto; padding-top:2rem;">Configuración</div>
    <button class="nav-item" onclick="navigate('config', this)">
      <span class="nav-icon">⚙</span> API key
    </button>
  </aside>

  <!-- Main content -->
  <main class="main">

    <!-- TABLERO -->
    <div id="page-tablero" class="page active">
      <div class="page-header">
        <div>
          <div class="page-title">Tablero</div>
          <div class="page-subtitle" id="today-date"></div>
        </div>
        <button class="btn-primary" onclick="openModal()">
          + Nuevo proyecto
        </button>
      </div>

      <div class="metrics">
        <div class="metric-card"><div class="metric-label">Proyectos activos</div><div class="metric-val" id="m-total">0</div></div>
        <div class="metric-card"><div class="metric-label">Lidero</div><div class="metric-val" id="m-leader">0</div></div>
        <div class="metric-card"><div class="metric-label">Colaboro</div><div class="metric-val" id="m-collab">0</div></div>
        <div class="metric-card"><div class="metric-label">Tareas pendientes</div><div class="metric-val" id="m-tasks">0</div></div>
      </div>

      <div id="projects-list" class="projects-grid"></div>
      <div id="empty-msg" class="empty-state" style="display:none">
        <div class="empty-icon">◻</div>
        <div class="empty-text">Todavía no hay proyectos. Usá el botón + para agregar el primero.</div>
      </div>
    </div>

    <!-- AGENDA -->
    <div id="page-agenda" class="page">
      <div class="page-header">
        <div>
          <div class="page-title">Agenda</div>
          <div class="page-subtitle">Eventos y fechas clave de tus proyectos</div>
        </div>
      </div>

      <div id="agenda-list"></div>

      <div class="add-event-form">
        <div class="add-event-title">Agregar evento</div>
        <div class="event-input-row">
          <input type="text" class="form-input" id="ev-title" placeholder="Título del evento" />
          <input type="date" class="form-input" id="ev-date" style="flex:1; min-width:130px;" />
          <input type="time" class="form-input" id="ev-time" style="flex:1; min-width:100px;" />
          <select class="form-select" id="ev-project" style="flex:1; min-width:120px;">
            <option value="">Sin proyecto</option>
          </select>
          <button class="btn-primary" onclick="addEvent()">Agregar</button>
        </div>
      </div>
    </div>

    <!-- ASISTENTE -->
    <div id="page-asistente" class="page">
      <div class="page-header">
        <div>
          <div class="page-title">Asistente</div>
          <div class="page-subtitle">Preguntame sobre tus proyectos, agenda o tareas</div>
        </div>
      </div>

      <div id="api-key-notice" class="api-notice" style="display:none">
        ⚠ Para usar el asistente necesitás configurar tu API key de Anthropic.
        <a href="#" onclick="navigate('config', null); return false;">Configurar →</a>
      </div>

      <div class="chat-container">
        <div class="chat-messages" id="chat-messages">
          <div class="msg msg-assistant">
            <div class="msg-bubble">¡Hola! Soy tu asistente de proyectos. Puedo ayudarte a revisar el estado de todo lo que tenés en curso, recordarte pendientes o ayudarte a priorizar. ¿Por dónde empezamos?</div>
            <div class="msg-time">Ahora</div>
          </div>
        </div>
        <div class="chat-quick">
          <button class="quick-chip" onclick="quickSend('Haceme un resumen de todos mis proyectos activos')">Resumen general</button>
          <button class="quick-chip" onclick="quickSend('¿Qué tareas tengo pendientes?')">Pendientes</button>
          <button class="quick-chip" onclick="quickSend('¿Qué proyectos necesitan atención urgente?')">Urgentes</button>
          <button class="quick-chip" onclick="quickSend('¿Cuáles son mis próximos eventos?')">Próximos eventos</button>
        </div>
        <div class="chat-input-area">
          <input type="text" class="form-input" id="chat-input" placeholder="Escribí tu consulta..." onkeydown="if(event.key==='Enter') sendMessage()" />
          <button class="chat-send" onclick="sendMessage()">Enviar</button>
        </div>
      </div>
    </div>

    <!-- CONFIG -->
    <div id="page-config" class="page">
      <div class="page-header">
        <div>
          <div class="page-title">Configuración</div>
          <div class="page-subtitle">API key para el asistente conversacional</div>
        </div>
      </div>
      <div style="background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 1.5rem; max-width: 480px;">
        <div class="form-group">
          <label class="form-label">Anthropic API key</label>
          <input type="password" class="form-input" id="api-key-input" placeholder="sk-ant-..." />
          <div style="font-size: 12px; color: var(--text-tertiary); margin-top: 6px;">
            Obtenela en <a href="https://console.anthropic.com" target="_blank" style="color: var(--accent);">console.anthropic.com</a>. Se guarda solo en tu navegador (localStorage).
          </div>
        </div>
        <button class="btn-primary" onclick="saveApiKey()">Guardar key</button>
        <div id="key-saved" style="display:none; font-size:13px; color:var(--green); margin-top:8px;">✓ API key guardada</div>
      </div>
    </div>

  </main>
</div>

<!-- Mobile bottom nav -->
<nav class="mobile-nav">
  <button class="mobile-nav-item active" onclick="navigate('tablero', this)">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>
    Tablero
  </button>
  <button class="mobile-nav-item" onclick="navigate('agenda', this)">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/></svg>
    Agenda
  </button>
  <button class="mobile-nav-item" onclick="navigate('asistente', this)">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
    Asistente
  </button>
  <button class="mobile-nav-item" onclick="navigate('config', this)">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="3"/><path d="M12 2v2m0 16v2M4.22 4.22l1.42 1.42m12.72 12.72 1.42 1.42M2 12h2m16 0h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>
    Config
  </button>
</nav>

<!-- Modal nuevo proyecto -->
<div class="modal-backdrop" id="modal" onclick="if(event.target===this)closeModal()">
  <div class="modal">
    <div class="modal-header">
      <div class="modal-title">Nuevo proyecto</div>
      <button class="modal-close" onclick="closeModal()">✕</button>
    </div>
    <div class="form-group">
      <label class="form-label">Nombre del proyecto</label>
      <input type="text" class="form-input" id="p-name" placeholder="Ej: Informe anual, Campaña Q3..." />
    </div>
    <div class="form-group">
      <label class="form-label">Mi rol en este proyecto</label>
      <select class="form-select" id="p-role">
        <option value="leader">Líder / responsable principal</option>
        <option value="collab">Colaboradora activa</option>
      </select>
    </div>
    <div class="form-group">
      <label class="form-label">Estado actual</label>
      <select class="form-select" id="p-status">
        <option value="active">En curso</option>
        <option value="review">En revisión</option>
        <option value="paused">Pausado</option>
      </select>
    </div>
    <div class="form-group">
      <label class="form-label">Próximo paso o tarea pendiente</label>
      <textarea class="form-textarea" id="p-task" placeholder="Ej: Enviar borrador antes del viernes"></textarea>
    </div>
    <div class="form-group">
      <label class="form-label">Progreso estimado</label>
      <div class="progress-row">
        <input type="range" id="p-progress" min="0" max="100" value="20" step="5" oninput="document.getElementById('prog-out').textContent=this.value+'%'" />
        <span id="prog-out">20%</span>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn-secondary" onclick="closeModal()">Cancelar</button>
      <button class="btn-primary" onclick="saveProject()">Guardar proyecto</button>
    </div>
  </div>
</div>

<script>
  // ── State ──────────────────────────────────────────────
  let projects = JSON.parse(localStorage.getItem('projects') || '[]');
  let events   = JSON.parse(localStorage.getItem('events')   || '[]');

  // ── Navigation ─────────────────────────────────────────
  function navigate(page, el) {
    document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
    document.getElementById('page-' + page).classList.add('active');
    document.querySelectorAll('.nav-item, .mobile-nav-item').forEach(n => n.classList.remove('active'));
    if (el) el.classList.add('active');
    if (page === 'tablero') render();
    if (page === 'agenda') renderAgenda();
    if (page === 'asistente') checkApiKey();
  }

  // ── Persist ────────────────────────────────────────────
  function save() {
    localStorage.setItem('projects', JSON.stringify(projects));
    localStorage.setItem('events',   JSON.stringify(events));
  }

  // ── Modal ──────────────────────────────────────────────
  function openModal() { document.getElementById('modal').classList.add('open'); }
  function closeModal() {
    document.getElementById('modal').classList.remove('open');
    document.getElementById('p-name').value = '';
    document.getElementById('p-task').value = '';
    document.getElementById('p-progress').value = 20;
    document.getElementById('prog-out').textContent = '20%';
  }

  function saveProject() {
    const name = document.getElementById('p-name').value.trim();
    if (!name) { document.getElementById('p-name').focus(); return; }
    const task = document.getElementById('p-task').value.trim();
    const proj = {
      id: Date.now(),
      name,
      role:     document.getElementById('p-role').value,
      status:   document.getElementById('p-status').value,
      progress: parseInt(document.getElementById('p-progress').value),
      tasks:    task ? [{ text: task, done: false }] : []
    };
    projects.push(proj);
    save();
    closeModal();
    render();
    syncAgendaSelect();
  }

  // ── Render tablero ─────────────────────────────────────
  function statusLabel(s) { return { active:'En curso', review:'En revisión', paused:'Pausado' }[s] || s; }
  function statusDotClass(s) { return { active:'dot-active', review:'dot-review', paused:'dot-paused' }[s] || ''; }
  function progressColor(p) { return p < 40 ? 'var(--amber)' : p < 70 ? 'var(--accent)' : 'var(--green)'; }

  function render() {
    const list  = document.getElementById('projects-list');
    const empty = document.getElementById('empty-msg');

    if (!projects.length) {
      list.innerHTML = '';
      empty.style.display = 'block';
    } else {
      empty.style.display = 'none';
    }

    let pending = 0;
    list.innerHTML = projects.map(p => {
      const undone = p.tasks.filter(t => !t.done).length;
      pending += undone;
      const taskRows = p.tasks.map((t, i) => `
        <div class="task-row ${t.done ? 'done' : ''}">
          <input type="checkbox" id="tk${p.id}-${i}" ${t.done ? 'checked' : ''} onchange="toggleTask(${p.id},${i})">
          <label class="task-label" for="tk${p.id}-${i}">${escHtml(t.text)}</label>
        </div>`).join('');

      return `
        <div class="project-card">
          <div class="card-top">
            <div class="card-title-row">
              <span class="status-dot ${statusDotClass(p.status)}"></span>
              <span class="card-title">${escHtml(p.name)}</span>
            </div>
            <span class="badge ${p.role === 'leader' ? 'badge-leader' : 'badge-collab'}">
              ${p.role === 'leader' ? 'Lidero' : 'Colaboro'}
            </span>
          </div>
          <div class="card-meta">
            <span class="card-meta-item">◷ ${statusLabel(p.status)}</span>
            <span class="card-meta-item">✓ ${undone} pendiente${undone !== 1 ? 's' : ''}</span>
            <span class="card-meta-item">${p.progress}% completado</span>
          </div>
          <div class="progress-track">
            <div class="progress-fill" style="width:${p.progress}%; background:${progressColor(p.progress)};"></div>
          </div>
          <div class="tasks-list">${taskRows}</div>
          <div class="card-actions">
            <button class="btn-ghost-sm" onclick="addTask(${p.id})">+ Tarea</button>
            <button class="btn-ghost-sm" onclick="updateProgress(${p.id})">↑ Progreso</button>
            <button class="btn-ghost-sm" onclick="deleteProject(${p.id})" style="margin-left:auto; color:var(--red);">Eliminar</button>
          </div>
        </div>`;
    }).join('');

    document.getElementById('m-total').textContent  = projects.length;
    document.getElementById('m-leader').textContent = projects.filter(p => p.role === 'leader').length;
    document.getElementById('m-collab').textContent = projects.filter(p => p.role === 'collab').length;
    document.getElementById('m-tasks').textContent  = pending;
  }

  function toggleTask(pid, idx) {
    const p = projects.find(p => p.id === pid);
    if (p) { p.tasks[idx].done = !p.tasks[idx].done; save(); render(); }
  }

  function addTask(pid) {
    const text = prompt('Nueva tarea:');
    if (!text || !text.trim()) return;
    const p = projects.find(p => p.id === pid);
    if (p) { p.tasks.push({ text: text.trim(), done: false }); save(); render(); }
  }

  function updateProgress(pid) {
    const p = projects.find(p => p.id === pid);
    if (!p) return;
    const val = prompt(`Nuevo progreso para "${p.name}" (0-100):`, p.progress);
    if (val === null) return;
    const n = Math.min(100, Math.max(0, parseInt(val) || 0));
    p.progress = n; save(); render();
  }

  function deleteProject(pid) {
    if (!confirm('¿Eliminar este proyecto?')) return;
    projects = projects.filter(p => p.id !== pid);
    save(); render(); syncAgendaSelect();
  }

  // ── Agenda ─────────────────────────────────────────────
  function addEvent() {
    const title = document.getElementById('ev-title').value.trim();
    const date  = document.getElementById('ev-date').value;
    if (!title || !date) return;
    events.push({
      id: Date.now(),
      title,
      date,
      time:    document.getElementById('ev-time').value,
      project: document.getElementById('ev-project').value
    });
    events.sort((a, b) => (a.date + a.time) < (b.date + b.time) ? -1 : 1);
    save();
    document.getElementById('ev-title').value = '';
    document.getElementById('ev-date').value  = '';
    document.getElementById('ev-time').value  = '';
    renderAgenda();
  }

  function renderAgenda() {
    const list = document.getElementById('agenda-list');
    if (!events.length) {
      list.innerHTML = '<div class="empty-state"><div class="empty-icon">◷</div><div class="empty-text">No hay eventos cargados. Agregá uno abajo.</div></div>';
      return;
    }
    const today = new Date().toISOString().slice(0, 10);
    const upcoming = events.filter(e => e.date >= today);
    const past     = events.filter(e => e.date < today);

    let html = '';
    if (upcoming.length) {
      html += '<div class="agenda-section-title">Próximos</div>';
      html += upcoming.map(e => eventRow(e)).join('');
    }
    if (past.length) {
      html += '<div class="agenda-section-title" style="margin-top:1.5rem;">Pasados</div>';
      html += past.slice(-5).reverse().map(e => eventRow(e)).join('');
    }
    list.innerHTML = html;
  }

  function eventRow(e) {
    const dateObj = new Date(e.date + 'T12:00');
    const dateStr = dateObj.toLocaleDateString('es-AR', { weekday:'short', day:'numeric', month:'short' });
    const proj = projects.find(p => p.id === parseInt(e.project));
    return `<div class="event-row">
      <div class="event-time">${dateStr}${e.time ? '<br>' + e.time : ''}</div>
      <div class="event-body">
        <div class="event-title">${escHtml(e.title)}</div>
        ${proj ? `<div class="event-sub">${escHtml(proj.name)}</div>` : ''}
      </div>
    </div>`;
  }

  function syncAgendaSelect() {
    const sel = document.getElementById('ev-project');
    if (!sel) return;
    const current = sel.value;
    sel.innerHTML = '<option value="">Sin proyecto</option>' +
      projects.map(p => `<option value="${p.id}" ${current == p.id ? 'selected' : ''}>${escHtml(p.name)}</option>`).join('');
  }

  // ── Chat / Asistente ───────────────────────────────────
  function checkApiKey() {
    const key = localStorage.getItem('anthropic_api_key');
    document.getElementById('api-key-notice').style.display = key ? 'none' : 'flex';
  }

  function saveApiKey() {
    const key = document.getElementById('api-key-input').value.trim();
    if (!key) return;
    localStorage.setItem('anthropic_api_key', key);
    document.getElementById('key-saved').style.display = 'block';
    checkApiKey();
  }

  function buildContext() {
    const today = new Date().toLocaleDateString('es-AR', { weekday:'long', year:'numeric', month:'long', day:'numeric' });
    if (!projects.length) return `Hoy es ${today}. No hay proyectos cargados.`;
    const projSummary = projects.map(p => {
      const pending = p.tasks.filter(t => !t.done);
      return `- "${p.name}": ${p.role === 'leader' ? 'lidera' : 'colabora'}, estado: ${statusLabel(p.status)}, ${p.progress}% completado, ${pending.length} tarea(s) pendiente(s)${pending.length ? ': ' + pending.map(t => t.text).join(', ') : ''}.`;
    }).join('\n');
    const today2 = new Date().toISOString().slice(0, 10);
    const upcoming = events.filter(e => e.date >= today2).slice(0, 5);
    const evSummary = upcoming.length
      ? 'Próximos eventos: ' + upcoming.map(e => `${e.title} el ${e.date}${e.time ? ' a las ' + e.time : ''}`).join(', ') + '.'
      : 'Sin eventos próximos cargados.';
    return `Hoy es ${today}.\n\nProyectos:\n${projSummary}\n\n${evSummary}`;
  }

  let chatHistory = [];

  async function sendMessage() {
    const input = document.getElementById('chat-input');
    const text  = input.value.trim();
    if (!text) return;
    input.value = '';

    const key = localStorage.getItem('anthropic_api_key');
    if (!key) {
      appendMsg('assistant', 'Necesitás configurar tu API key primero. Andá a Configuración → API key.');
      return;
    }

    appendMsg('user', text);
    chatHistory.push({ role: 'user', content: text });

    const thinking = document.createElement('div');
    thinking.className = 'chat-thinking';
    thinking.id = 'thinking';
    thinking.textContent = 'Pensando...';
    document.getElementById('chat-messages').appendChild(thinking);
    scrollChat();

    try {
      const systemPrompt = `Sos una asistente secretarial eficiente y concisa. Ayudás a la usuaria a hacer seguimiento de sus proyectos, recordarle tareas y organizar su agenda. Respondé siempre en español rioplatense (tuteo). Sé directa y útil, sin preambles innecesarios.\n\nContexto actual:\n${buildContext()}`;

      const response = await fetch('https://api.anthropic.com/v1/messages', {
        method: 'POST',
        headers: {
          'Content-Type':      'application/json',
          'x-api-key':         key,
          'anthropic-version': '2023-06-01'
        },
        body: JSON.stringify({
          model:      'claude-sonnet-4-6',
          max_tokens: 1000,
          system:     systemPrompt,
          messages:   chatHistory
        })
      });

      const data = await response.json();
      document.getElementById('thinking')?.remove();

      if (data.error) {
        appendMsg('assistant', 'Error: ' + data.error.message);
        chatHistory.pop();
        return;
      }

      const reply = data.content.map(b => b.text || '').join('');
      chatHistory.push({ role: 'assistant', content: reply });
      appendMsg('assistant', reply);

    } catch (err) {
      document.getElementById('thinking')?.remove();
      appendMsg('assistant', 'No pude conectarme. Revisá tu conexión y la API key.');
      chatHistory.pop();
    }
  }

  function appendMsg(role, text) {
    const now = new Date().toLocaleTimeString('es-AR', { hour:'2-digit', minute:'2-digit' });
    const div = document.createElement('div');
    div.className = `msg msg-${role}`;
    div.innerHTML = `<div class="msg-bubble">${escHtml(text)}</div><div class="msg-time">${now}</div>`;
    document.getElementById('chat-messages').appendChild(div);
    scrollChat();
  }

  function scrollChat() {
    const el = document.getElementById('chat-messages');
    el.scrollTop = el.scrollHeight;
  }

  function quickSend(text) {
    document.getElementById('chat-input').value = text;
    sendMessage();
  }

  // ── Helpers ────────────────────────────────────────────
  function escHtml(s) {
    return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
  }

  // ── Init ───────────────────────────────────────────────
  document.getElementById('today-date').textContent =
    new Date().toLocaleDateString('es-AR', { weekday:'long', year:'numeric', month:'long', day:'numeric' });

  render();
  syncAgendaSelect();
</script>
</body>
</html>
